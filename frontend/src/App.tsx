/**
 * Raiz de la aplicacion.
 *
 * TODO(A6): router, QueryClientProvider, guard de autenticacion y layout.
 *
 * Organizacion: por dominio (`features/`), no por tipo de archivo. Cada fase
 * añade una carpeta nueva en lugar de dispersar archivos por `components/`,
 * `hooks/` y `pages/`.
 */
export default function App() {
  return (
    <main>
      <h1>firewall-dashboard</h1>
      <p>Scaffolding inicial. Ver docs/ARCHITECTURE.md §8.</p>
    </main>
  );
}
